# unique-shopify-product-html-tags

Extracts a list of unique html tags from Shopify product `body_html` descriptions in JSON dump files, and writes them to a CSV.

`npm start {path to dir of JSON files}`